<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

    <h2>Users</h2>
    
    <ul>
       
            <% for (var i=0; i < users.length; i++) { %>
                <li>
            <h3><img src="<%= users[i].picture.thumbnail %>" > <%= users[i].name.first %> <%= users[i].name.last %> </h3>
            </li>
        <% } %>
    </ul>
</body>
</html>